server:
  port: 8091
spring:
 cloud:
  config:
   server:
    git:
     uri: https://github.com/luegmy/GageMicroservicios
     searchPaths: config-data
     username: ${GIT_USER}
     password: ${GIT_PASSWORD}
     default-label: "master"
 security:
    user:
      name: root
      password: s3cr3t
     
  
