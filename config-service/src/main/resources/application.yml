server:
  port: 8090
  
spring:
 cloud:
  config:
   server:
    git:
     uri: https://github.com/luegmy/GageMicroservicios.git
     searchPaths: config-data
     username: ${GIT_USER}
     password: ${GIT_PASSWORD}
     default-label: "master"
 application:
    name: 
 security:
    user:
      name: root
      password: s3cr3t
     
  
